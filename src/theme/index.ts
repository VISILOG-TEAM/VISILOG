import { colors } from './colors';
import { typeScale, fonts } from './typography';
import { spacing, radius } from './spacing';
import { shadows } from './shadows';

// Convenience aggregate: `import { theme } from '../theme'`
export const theme = { colors, type: typeScale, fonts, spacing, radius, shadows };

export { colors, typeScale, fonts, spacing, radius, shadows };
